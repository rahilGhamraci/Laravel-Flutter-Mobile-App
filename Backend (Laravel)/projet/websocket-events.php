<?php
use BeyondCode\LaravelWebSockets\Facades\Websocket;


/*Websocket::on('message', function ($payload, $sessionId) {
    echo "Received message: " . json_encode($payload) . "\n";
    // Broadcast the message to the appropriate channels
   // Websocket::broadcastToRoom($payload['conversation_id'], 'message', $payload);

    echo "Sent message: " . json_encode($payload) . "\n";
});*/

